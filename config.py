# This file holds the central configuration for our agent.
# It's the single source of truth to avoid circular imports.

# The dev_team is now the 'executor_team'
VALID_TEAMS = ["researcher_team", "coder_team", "executor_team", "social_media_team", "finance_team"]